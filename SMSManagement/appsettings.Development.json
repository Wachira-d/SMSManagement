{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "_comment": "Auto-create the campaign database on first startup if it doesn't exist (SQL #4060).",
    "AutoCreateDatabase": true
  },
  "Coupon": {
    "PublicBaseUrl": "https://localhost:5001"
  },
  "Secrets": {
    "_comment": "Auto-generate missing crypto secrets (password pepper, AES data key, IP hash salt, JWT signing key) and persist to dev-secrets.json. Dev only — production must inject from Key Vault.",
    "AutoGenerateInDev": true
  },
  "Bootstrap": {
    "_comment": "Dev break-glass admin. On every startup the bootstrapper rotates this account's password to match the value here (and unlocks it). Plaintext here is acceptable ONLY because this file is appsettings.Development.json — never copy these values into appsettings.json. In production the admin is provisioned via Key Vault / env vars and this section stays blank.",
    "AdminUsername": "admin",
    "AdminPassword": "Admin@Dev2026",
    "AdminEmail": "admin@local.dev",
    "_promoteComment": "Email / username strings here get IsSystemAdmin=true on every boot. Idempotent and ADDITIVE — entries are never demoted by this list; to revoke admin, toggle via the /Admin/Users page. Keep this array empty unless you specifically need a config-driven admin promotion.",
    "PromoteToSystemAdmin": []
  }
}
