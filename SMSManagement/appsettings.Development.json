{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "_comment": "Auto-create the campaign database on first startup if it doesn't exist (SQL #4060). Dev only — never honoured outside the Development environment.",
    "AutoCreateDatabaseInDev": true
  }
}
